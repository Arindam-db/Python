# input() is a function that prompts the user for input and returns the data as a string 

name = input("what is your name ? ")
age = int(input("how old are you ? "))
age += 1

print(f"Hello {name} " )
print(f"you are {age} years old")